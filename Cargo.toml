[package]
name = "pgrust-nextest-repro"
version = "0.1.0"
edition = "2024"
publish = false
build = "build.rs"

[dependencies]
anyhow = "1"
tokio = { version = "1", features = ["macros", "rt", "time"] }
tokio-postgres = "0.7"
